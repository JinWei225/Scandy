# Backend address used by the native (Capacitor) app. The web build uses
# relative /api paths (proxied by the dev server / nginx) and ignores this.
# Change this instead of editing App.vue when the Tailscale IP changes.
VUE_APP_NATIVE_API_URL=http://100.69.155.6:5001
