<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Receipt capture. `required=false` keeps the app installable on devices
         without a camera; image_picker falls back to the gallery. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!-- No networkSecurityConfig, deliberately. One used to allow cleartext
         app-wide for a Flask backend reached over plain HTTP on the LAN; that
         backend is gone and everything now goes to Supabase over HTTPS, so
         Android's default -- cleartext blocked since API 28 -- is the right one. -->
    <application
        android:label="Scandy"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Share-to-Scandy. This is the port of the Capacitor build's
                 send-intent plugin: sharing a receipt photo from Gallery,
                 Photos or a chat app hands it straight to the scanner.
                 SEND is one image, SEND_MULTIPLE a batch. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="image/*"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="image/*"/>
            </intent-filter>

            <!-- Password reset and email confirmation. Supabase sends a link
                 that ends up redirecting to this scheme; without the filter it
                 opens a browser tab, which cannot complete the reset because
                 the recovery session belongs to the app, not the browser.

                 The scheme must match the redirectTo passed to
                 resetPasswordForEmail() in Dart, and must be listed under
                 Supabase > Authentication > URL Configuration > Redirect URLs.
                 launchMode="singleTop" above is what keeps this from opening a
                 second copy of the app on top of the running one. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="com.jinwei.scandy" android:host="login-callback"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
